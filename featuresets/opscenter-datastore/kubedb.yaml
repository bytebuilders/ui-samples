apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kubedb
  namespace: kubeops
spec:
  interval: 30m
  releaseName: kubedb
  targetNamespace: kubedb
  chart:
    spec:
      chart: kubedb
      version: "v2023.02.28"
      sourceRef:
        kind: HelmRepository
        name: kubedb
        namespace: kubeops
  install:
    createNamespace: true
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
