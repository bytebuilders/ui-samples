apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kubedb-provider-aws
  namespace: kubeops
  labels:
    ace.appscode.com/feature: kubedb-provider-aws
spec:
  interval: 5m
  timeout: 30m
  releaseName: kubedb-provider-aws
  targetNamespace: crossplane-system
  storageNamespace: crossplane-system
  chart:
    spec:
      chart: kubedb-provider-aws
      version: "v2023.12.11"
      sourceRef:
        kind: HelmRepository
        name: appscode-charts-oci
        namespace: kubeops
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: -1
  upgrade:
    crds: CreateReplace
    remediation:
      retries: -1
