apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: stash
  namespace: kubeops
spec:
  interval: 5m
  timeout: 10m
  releaseName: stash
  targetNamespace: stash
  chart:
    spec:
      chart: stash
      version: "v2023.02.28"
      sourceRef:
        kind: HelmRepository
        name: stashed
        namespace: kubeops
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: 5
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 5
  values:
    features:
      enterprise: true
