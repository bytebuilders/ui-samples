apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kubestash
  namespace: kubeops
  labels:
    ace.appscode.com/feature: kubestash
spec:
  interval: 5m
  timeout: 30m
  releaseName: kubestash
  targetNamespace: kubestash
  storageNamespace: kubestash
  chart:
    spec:
      chart: kubestash
      version: "v2023.10.30-rc.0"
      sourceRef:
        kind: HelmRepository
        name: appscode-charts-oci
        namespace: kubeops
  install:
    createNamespace: true
    crds: CreateReplace
    remediation:
      retries: -1
  upgrade:
    crds: CreateReplace
    remediation:
      retries: -1
  values:
    features:
      enterprise: true
